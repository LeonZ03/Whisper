# Whisper CLI：PowerShell 命令安装与聊天

## 服务提供者

双击工程根目录 `start.cmd`。本机服务及 Cloudflare 临时隧道仍是同一个实例，不需要另开聊天服务器。
启动时自动检查客户端发布包；源码、依赖或打包规则变化才重建，无变化则复用校验过的包。
窗口会显示本机网址、公网网址，以及 **朋友用的安装/更新命令、连接命令和安装帮助页**。
同样的信息写到 `data/access-info.txt`；CLI 命令单独写到 `data/cli-commands.txt`，方便发给朋友。
朋友只能使用公网 HTTPS 地址；他电脑上的 `127.0.0.1` 指向他自己。新流程不使用邀请码，账号申请由所有者审批。

## 朋友：首次安装

在自己的 Windows x64 PowerShell 中执行服务提供者启动窗口里的完整安装命令。
也可以打开服务公网网址下的 `/cli.html`，点击“复制安装命令”。这是帮助页，不再要求手动下载和解压。
命令会下载脚本、核对脚本 SHA-256，在独立 PowerShell 进程中运行，然后下载、校验和安装客户端。
安装到 `%LOCALAPPDATA%\WhisperCLI`，添加当前用户的 `bin` 到用户 PATH，同时刷新当前命令窗口的 PATH。
不要求管理员权限，不全局安装 Node.js/npm，不安装系统服务，不修改机器级或用户级持久执行策略。
脚本调用的 `-ExecutionPolicy Bypass` 仅用于该子进程；组织策略阻止执行时应遵守组织要求，不关闭防护软件。
只执行你信任的提供者给出的命令。校验哈希不是独立的发布者签名，不能防止恶意发布者。

## 朋友：日常连接

```powershell
whisper --server https://服务提供者给出的当前地址
# 沿用安装时保存的服务器：
whisper
# 卸载客户端，默认保留公钥核对记录：
whisper --uninstall
```
将示例网址换成真实网址。临时地址变化只需使用新的 `--server`，不用重新安装。
更新客户端时重新执行最新安装命令；重复安装会检查现有文件，不覆盖公钥核对记录。
安装命令在其执行的当前窗口刷新 PATH。其他已打开的终端可重新打开，或用完整路径 `%LOCALAPPDATA%\WhisperCLI\bin\whisper.cmd` 启动。
CLI 与网页账号互通，服务提供者关机、断网或停止隧道时，两种客户端都会失去连接。

## 密码与账号申请

首次 `/register` 会提交账号申请；申请说明可由管理员和服务端读取，不要填写密码或敏感信息。申请获批后再 `/login`。新密码、改密和成员恢复设置的新密码均限 1–12 个 Unicode NFC 规范化码点；登录仍接受现有账号的旧长密码，直至用户主动改密。密码在本机交互提示中输入，不支持命令行参数。

`/passwd` 修改密码时会用新派生密钥重新加密同一身份私钥，联系人安全码保持不变。`/recover` 需输入管理员私下提供的一次性恢复码，会创建新身份并封存旧会话；旧消息不能用新身份解密，需重新与联系人核对安全码。root 管理员只能在网页登录和维护。

## 聊天操作

| 命令 | 功能 |
| --- | --- |
| `/login`、`/register` | 登录、提交账号申请；root 管理需使用网页 |
| `/passwd`、`/recover` | 修改密码并保留身份；或用管理员恢复码重建身份 |
| `/chats`、`/chat 用户名` | 列出会话、选择双人会话 |
| `/safety` | 核对双方安全码，首次记住公钥不等于已验证身份 |
| `/ttl 1m` | 新消息保留时间；也支持 1h、24h、7d |
| `/delete 12`、`/clear` | 删除 #12 消息、清空双方记录；需要确认 |
| `/refresh`、`/web` | 刷新、查看对应网页地址 |
| `/server https://...` | 未登录时切换服务器，不合并不同服务的数据 |
| `/logout`、`/quit`、`/help` | 退出账号、退出 CLI、帮助 |

进入会话后直接打字，Enter 发送，Ctrl+J 换行。
输入 `/` 后，输入行下方立即显示带说明的命令列表；继续输入会按前缀筛选。
↑↓ 选择，Enter 确认；需要参数的命令先补全并等待输入参数，无参数命令进入对应操作。
Tab 只补全，Esc 收起列表且保留已输入内容；密码提示、多行草稿、粘贴和 `//` 普通文字不会触发菜单。

鼠标滚轮、PageUp/PageDown 可连续翻阅；未打开菜单时 ↑↓ 切换本次会话的上一条/下一条有效命令，回到末尾恢复原草稿。
滚到已加载顶部会按 200 条一页加载更早的仍有效消息，不再只保留最近 200 条。
Ctrl+Home 到当前已加载顶部，Ctrl+End 回到最新消息；向上阅读时新消息只显示提示，不拉回底部、不覆盖草稿。
这是应用内历史视图，不是把明文永久写入 PowerShell 滚屏或磁盘；消息删除、到期后仍同步清理。
服务器原有保留时间与总量上限继续生效，已删除或到期的内容不能通过翻阅找回。
支持括号粘贴的终端里，多行粘贴不会自动执行斜杠命令。
要求真实交互终端，不支持 PowerShell ISE、管道输入或重定向输出；`--help`、`--version` 除外。
客户端只支持文字收发；图片占位提示不会消耗阅后查看机会，请网页登录同一账号查看。

## 安装结构与安全边界

`bin/whisper.cmd` 是命令入口；`versions/<sha256>/Whisper-CLI` 包含客户端及独立运行时；
`settings.json` 只保存默认服务地址，`installed.json` 记录已安装版本；`data/cli-pins.json` 保存公钥核对状态。
升级、重复安装和卸载不清空 `data/`。旧的手动便携包自行运行时产生的公钥记录不会被自动搬移；保留原文件，重新核对安全码。
复用 `src/crypto.mjs` 和现有 API，明文只在使用者本机解密。安装包不包含主机数据库、root 激活码、私钥或 Cloudflare 凭据。
密码、解锁私钥、Cookie、聊天明文和命令历史均不写入文件。有效命令最多在当前会话内存保留 100 条；不记录聊天正文或敏感提示输入。公钥记录仍是私人元数据，不要转发整个用户目录。
终端转义控制符会被过滤，但清屏不能删除终端转录、截图、系统层副本。本原型未经审计，无前向保密。
没有通过 SSH 远程登录主机，也没有改变固定双人授权、加密协议或服务端数据库格式。

## 开发维护

本工程内仍可运行 `.\whisper.cmd` 或 `npm.cmd run cli`，默认连接本机服务；这是开发入口，不是朋友的安装流程。
Windows x64 发布包仍是必要的内部下载载荷，位于 `public/downloads/`，不提交 Git，也不用于备份工程。
`client-distribution/install.ps1` 是安装脚本源，`public/install.ps1` 是构建产物。
`public/cli-command.mjs` 同时供启动器和帮助页使用，保证它们生成的命令一致。

```powershell
npm.cmd run build:cli        # 检查并生成客户端和安装脚本；-- --force 强制重建
npm.cmd test                # 原有加密、接口和 CLI 测试
npm.cmd run build
npm.cmd run test:e2e         # 网页及 CLI/网页互通
npm.cmd run test:cli:tty     # 开发客户端真实 PowerShell/ConPTY 测试
npm.cmd run test:cli:package # 工程外载荷完整性和独立运行时测试
npm.cmd run test:cli:install # 命令安装、重复安装、校验失败、安装后聊天、卸载和启动器
```

自动测试使用临时数据库/安装目录；安装测试以 `-NoPath` 禁止修改宿主用户持久 PATH，但验证安装命令对当前测试窗口 PATH 的刷新。
正常安装默认添加用户 PATH。本机测试结果可参阅未提交的 `CLI-VERIFICATION.md`，不把测试通过当成安全审计。
云端账号审批升级及发布状态见 `cloud/DEPLOYMENT.md`；须以实际迁移、推送与线上验收记录为准。使用正式域名不依赖提供者电脑；本机／临时隧道仍是独立实例。

参考：Microsoft Invoke-WebRequest、Get-FileHash、about_Execution_Policies；Cloudflare Quick Tunnels 官方文档。

## 0.2.0：历史翻阅和实时命令菜单

本次新增服务器的历史分页与历史状态查询接口；不改变数据库表结构、身份密钥、消息加密格式或图片领取规则。
旧网页客户端继续使用原有最近 200 条接口。新 CLI 登录旧服务时仍可聊天，但不能加载更早的分页。
服务提供者需要停止后重新运行 `start.cmd` 一次，使新的历史接口生效；临时公网地址可能随之变化。
已经通过命令安装的用户需退出 CLI，重新执行最新安装命令更新，再用 `whisper --version` 确认显示 `0.2.0`。
直接运行工程内 `.\whisper.cmd` 的开发入口只需重新打开 CLI。运行中的旧进程不会热更新。

CLI 加载的历史仍保存在当前进程内，正常同步时核对已加载范围的删除、过期与图片已查看状态。
滚轮操作不会领取阅后图片，新增历史接口仍验证当前用户必须是会话参与者。
翻阅时保留当前阅读位置和草稿，并提示底部有新消息；只重绘变化的行，避免轮询整屏闪烁。
历史仅指服务器仍保留的聊天记录，不代表无限保存，也不能找回已经清理的内容。

新回归文件 `tests/cli-history.test.mjs` 已纳入 `npm.cmd test` 和 `npm.cmd run test:cli`。
真实 PowerShell/ConPTY 测试增加命令菜单选择、超过 200 条的翻阅、滚轮、阅读锚点和旧页删除验证。

## 0.2.1：克制的终端配色

默认自动启用颜色，不修改 PowerShell / Windows Terminal 的全局主题或背景。
名字、命令、输入提示使用青蓝色；对方名字和成功状态使用绿色；未核对身份等警告为黄色，操作错误为红色。
时间、消息序号、分隔线和辅助说明弱化；正文保持终端默认前景色。菜单选中项用高对比反显，并保留箭头标记。
颜色使用终端调色板，具体色调取决于用户主题；未增加动画、渐变或大面积彩色背景。

安装后的客户端更新到 `whisper --version` 显示 `0.2.1` 即可；重新执行现有安装/更新命令，不必先卸载。
工程内 ` .\whisper.cmd ` 退出后重新打开即可看到。此轮只有客户端呈现变化，不要求重启聊天后端。
需要关闭颜色时执行 `whisper --no-color`（可同时指定 `--server`）；也遵守 `NO_COLOR`、`NODE_DISABLE_COLORS`、`TERM=dumb`。
`whisper --color` 可显式强制开启；仍要求真实交互终端。单色模式保留命令箭头、状态文字和所有操作。

颜色在安全文本过滤和宽度计算完成后添加，不参与滚动锚点或中文光标计算。
消息正文不会因为包含 `/clear`、状态词或 ANSI 转义字符而获得系统提示的样式；不是富文本或终端指令。
检查范围包括真实 PowerShell 色彩属性、单色回退、隐藏密码、命令菜单、历史翻页、草稿、跨网页互通和分发安装。
测试终端单元格快照位于 `test-results/`；`node tests/render-cli-preview.mjs` 可由测试快照生成深浅调色板预览，不读取真实聊天。

实现参考：Node.js TTY 色彩能力检测 https://nodejs.org/api/tty.html ；Codex CLI 终端定制 https://developers.openai.com/codex/cli-customization 。本项目并未复用 Codex 代码或账号服务。

## 0.3.0：命令回溯、连续输出与消息倒计时

没有打开命令菜单时，↑ 调出上一条有效命令，↓ 调出下一条；下移越过最新一条会恢复回溯前的草稿和光标。
从历史调出的 `/help` 等不会自动重新展开候选菜单，因此可以连续按上下键。手动继续编辑后可重新触发前缀匹配。
菜单展开时 ↑↓ 仍选择候选项；聊天翻阅改用滚轮、PageUp/PageDown、Ctrl+Home/Ctrl+End。
最多缓存 100 条经过语法校验的命令，只存在当前进程内存；连续重复项合并。退出账号、切换服务/身份或关闭 CLI 会清理。
聊天正文、密码、申请说明、交互确认答案以及含无效参数的命令不进入回溯列表；不将历史写入 PowerShell 或磁盘。

`/help`、`/chats`、`/web`、`/safety` 改为聊天时间线中的本机输出块，带有“仅本机”标记，不再用帮助内容替换聊天。
原消息仍可向上翻阅，之后收到的消息接在对应位置；直接输入可继续聊天，不必先退出帮助页。
命令输出不发送给对方，不复制消息明文；消息数据仍以同步结果为准，删除或到期后不会因为本机输出块而重新出现。
本机输出块按账号和会话隔离，最多保留最近 64 个，退出账号后清理；此限制不截断服务器的有效聊天历史。

每条有保留期限的消息末尾显示 `[剩余 00:00:59]`，不足一行时放到下一行；较长期限可显示天数。
倒计时按原消息 `expiresAt` 实时计算，约每秒变化；一分钟内用黄色、十秒内用红色提醒，单色模式仍显示完整文字。
倒计时只更新发生变化的终端行，不移动草稿光标，不为计时额外发起网络请求或每秒重新解密历史。
到期从当前视图移除；计时不会修改消息期限，也不是“延时发送”功能。显示受本机时钟准确性影响，服务端仍独立执行到期校验。
未查看图片显示的是等待领取的保留期限，不是网页打开后的 10 秒查看时间；CLI 仍不领取图片。

源码入口退出后重新打开即可。已安装客户端需重新执行现有安装/更新命令，并用 `whisper --version` 确认 `0.3.0`。
本次无需修改或重启后端；加密协议、数据库和隧道方式不变。
交互参考：Codex 官方 CLI 文档的 Up/Down 草稿历史与 slash popup 说明：
https://learn.chatgpt.com/docs/developer-commands?surface=cli
实现按聊天隐私要求只保留有效命令，不照搬通用终端的持久历史文件。

## 连接失败：浏览器和 PowerShell 同时打不开

2026-09-24 排查曾复现：本机首页及健康接口正常，cloudflared 已连接；Windows PowerShell 默认通过本地代理访问公网网址失败，独立 Edge 默认访问报 ERR_CONNECTION_CLOSED。同机显式直连后，PowerShell、curl、Node 和 Edge 均取得 HTTP 200。此结果只描述本次测试网络，不保证其他网络可直连。

先区分本机服务、主机到 Cloudflare 的隧道，以及访问者到公网地址的路线。隧道已连接不等于所有访问者都能打开网页。自己在主机上可使用 `http://127.0.0.1:8787`；朋友不能用这个回环地址。

安装命令、发布清单和客户端下载：默认请求发生无 HTTP 响应的网络错误时，会显示提示，使用 Windows 自带 curl.exe 进行一次不经过显式代理的重试。重试仅作用于公开安装资源请求，不修改系统代理、TLS 设置、用户 PATH 以外的安装环境或聊天协议。证书验证、禁止跳转和脚本/文件 SHA-256 校验继续生效；HTTP 错误及哈希不符不会因此被忽略。不要加入 --insecure 或关闭证书验证。

浏览器不受安装器重试影响，需要在当前代理软件内为目标域名选择可用路线。本次直连可用；Clash 类规则可以为 `trycloudflare.com` 添加 DIRECT 域名后缀规则，具体操作遵循所使用客户端，不能把这当作所有网络通用的保证。

粘贴到 PowerShell 的网址必须是纯 URL，不能带 Markdown 的 `[网址](网址)` 包装；变量应为 `$_` 而非 `$\_`。优先使用安装帮助页的“复制安装命令”按钮，或直接从 `data/cli-commands.txt` 复制。安装脚本更新后旧哈希命令会失效，必须复制最新命令，不要删除哈希检查。

参考：Cloudflare Tunnel common errors（https://developers.cloudflare.com/cloudflare-one/networks/connectors/cloudflare-tunnel/troubleshoot-tunnels/common-errors/）；curl --noproxy（https://curl.se/docs/manpage.html#--noproxy）。

## 正式云端入口

安装／更新：https://whisper.leonz03.dpdns.org/cli.html 。
连接命令：`whisper --server https://whisper.leonz03.dpdns.org`。
云端账号申请和审批独立于本机；两边不会同步账号、密钥或消息。
GitHub 推送更新云端服务与可下载客户端，不会静默替换已经安装的 CLI。
