# 安全机制与限制

Whisper 是小范围、本机及云端分离的双人聊天原型，未经独立安全审计。本文按当前 `src/crypto.mjs` 描述实现；不承诺匿名、前向保密、后量子保护、防截图或远程彻底擦除。

## 当前加密方式

- 密码先做 Unicode NFC 规范化。新密码按 Unicode 码点限制为 1–12 个；旧账号登录仍接受原有较长密码。客户端使用 PBKDF2-HMAC-SHA256、每账号随机 salt、600,000 次迭代，再以带不同用途标签的 HKDF-SHA256 得到认证派生值和身份 vault 密钥。
- 每个账号生成 X25519 密钥对。私钥以 libsodium `crypto_secretbox_easy` 加密后保存在服务端；解锁后客户端用 `crypto_box_easy` 将消息载荷加密给对方公钥。该原语由 X25519 密钥协商、XSalsa20 加密和 Poly1305 认证组成。每条消息都生成新的随机 24 字节 nonce；服务端仅收密文和 nonce。Base64 只是传输编码，不提供加密。
- 加密载荷包含版本、消息 ID、会话 ID、发送/接收账号、类型、到期时间和正文。服务端校验参与者权限、保留期限并转发密文。
- 双方安全码把两名用户按账号 ID 排序，连接各自的 `ID:公钥`，在前面加 `Whisper safety code v1|` 后做 SHA-256，再以每四位一组的十六进制显示。双方应通过当面或另一个可信渠道比较完整安全码；它不是聊天密码。首次固定公钥不代表身份已验证。
- 普通改密会用新派生密钥重新加密同一身份私钥，所以公钥和安全码不变。成员忘密恢复会换新身份密钥并封存旧会话；旧私钥不会由服务器恢复，旧消息不能通过新身份解密，联系人需重新核对安全码。

云端认证还对客户端派生的 authKey 使用独立运行时 pepper 做带用途分隔的 HMAC 校验。云端没有本机的额外 scrypt 工作因子。细节见 [云端认证说明](cloud/AUTHENTICATION.md)。

## 主要限制

- 没有 Double Ratchet、前向保密或后量子保护。长期私钥泄露可能危及仍可取得的历史密文；数据库加密密钥丢失则可能无法解锁身份。
- HTTPS/TLS 保护设备到服务入口的传输，端到端加密保护消息正文，即使 TLS 在服务入口终止，服务器仍不能凭数据库解开正常聊天正文。服务端数据库含账号公钥、加密身份私钥、认证校验数据、申请说明和密文；服务端及转发服务可观察 IP、时间、大小、通信关系、会话与到期元数据。root 登录功能会保留到达应用的登录尝试 IP 和估计位置；IP 可能只是代理出口，不是 GPS 或精确住址。
- 密码派生值和加密 vault 可能遭受离线猜测；1 字符密码很容易被猜中。云端额外 pepper 不能防止同时取得数据库与 pepper 的攻击者运行客户端 KDF 猜测密码。
- 账号删除、消息到期及界面清除作用于活动数据和界面，不承诺云厂商备份、数据库日志、终端记录、截图或收件方副本立即消失。
- Web 页面或客户端被篡改、设备中毒、恶意扩展、截屏录屏、操作系统或终端被控制，都可能暴露明文或密码。删除动画不是擦除保证。
- 安全码用于比较公钥，不是身份认证中心；公钥变化应暂停发送，先通过可信渠道确认。

请仅用非敏感内容，并自行保管密码、root 激活码和成员恢复码。任何复核或正式发布状态以 `cloud/DEPLOYMENT.md` 的实际记录为准。

算法名称以 [libsodium 的 `crypto_box` 文档](https://doc.libsodium.org/public-key_cryptography/authenticated_encryption)和 [`crypto_secretbox` 文档](https://doc.libsodium.org/secret-key_cryptography/secretbox)核对；Whisper 的组合方式和限制以上述项目源码为准。
